## DawooLISP 1.7.11 공개베타

### DWNUM 반경검색 직접 복사

- 반경검색 결과를 블록으로 삽입·분해하지 않고 DXF 일반 객체를 새 도면으로 직접 복사합니다.
- 사용자가 선택한 검색 중심점에 빨간색 3중 도넛 표식을 작성합니다.
- 도넛 내경/외경은 각각 `50/60`, `100/110`, `150/160`입니다.
- 여러 도엽이 결합되어도 검색 기준점을 쉽게 찾을 수 있습니다.

### DWJI 지번 자동 정리

- 사각영역 지적도를 원도면에서 생성한 뒤 `DEFST → DWJIA`를 자동 실행합니다.
- 문자 스타일과 지번 정리가 완료된 결과만 새 빈 도면으로 복사합니다.
- 새 도면 전환 뒤 LISP 명령을 다시 실행하지 않아 VL 네임스페이스 오류를 방지합니다.

### 결과 화면 자동 표시

- `DWNUM`과 `DWJI` 결과 도면을 활성화한 뒤 전체 재생성과 Zoom Extents를 적용합니다.
- 결과가 빈 화면처럼 보이지 않고 전체 객체가 바로 표시됩니다.

### DWNUM 사각영역 방식 유지

- 사각영역 검색은 기존처럼 새 도면의 블록참조에 XCLIP을 적용합니다.
- 사각영역 모드에서는 블록을 분해하지 않아 선택 영역 표시 상태를 유지합니다.

### 기존 핵심 구조 유지

- 1.7.10의 설치·업데이트·신뢰 위치·시작세트·PGP·메뉴 등록 방식을 변경하지 않았습니다.
- 기존 AutoCAD·ZWCAD 도구막대와 PNG 절대경로 구조를 유지했습니다.
- AutoCAD와 ZWCAD를 모두 종료한 뒤 `Install.exe`를 실행합니다.
- 공개베타 버전이므로 업데이트 채널을 베타버전으로 설정해야 자동 업데이트됩니다.
