## DawooLISP 1.7.17 공개베타

### DWOS 객체스냅 설정 통합

- 기존 `OSDEF`와 `OSED` 명령을 제거하고 `DWOS` 하나로 통합했습니다.
- `기본값` 버튼은 OSMODE `4271`을 선택합니다.
- `편집` 버튼은 OSMODE `2057`을 선택합니다.
- 각 객체스냅 항목을 직접 선택하거나 해제한 뒤 `확인`으로 적용할 수 있습니다.
- 실행 시 현재 도면의 OSMODE 항목을 창에 표시합니다.
- AutoCAD와 ZWCAD의 메뉴·도구막대 버튼을 `DWOS` 하나로 통합했습니다.
- 확정한 객체스냅 아이콘을 16×16 및 32×32 규격으로 적용했습니다.

### DWSTA 선형 측점 작성

- 기존 `DWNO` 명령을 `DWSTA`로 변경했습니다.
- STA/NO 형식, 측점·원·문자 간격, 선 길이, 시작 거리, 문자 위치·크기·소수 자리, 레이어와 색상을 설정 창에서 지정할 수 있습니다.
- STA와 NO의 기본 측점 간격은 모두 20m입니다.
- STA 종점의 소수값은 `0+155.123`처럼 소수점을 포함하고, 정수값은 점 없이 표시합니다.
- 문자 스타일은 현재 도면에 등록된 문자 스타일 목록에서 선택합니다.
- 주석형 선택 시 기존 DWNO 방식처럼 현재 주석축척 배율로 선·원·문자 크기를 조정합니다.
- 측점 심벌은 `CA-ALGN-STSM` 노랑, 문자는 `CA-ALGN-TEXT` 초록을 기본값으로 사용합니다.

### DEFST 문자 스타일

- 기존 `돋움체AT` 스타일 이름을 `일반문자AT`로 변경했습니다.
- `DEFST` 실행 시 `일반문자AT`를 실제 주석형 문자 스타일로 생성하거나 기존 설정을 교정합니다.

### 통합 설정 창 적용

- `DWNUM`, `DEF`, `DEFLA`, `DEFSC`, `DEFCTB`, `DWJI`, `DWSHP`, `DWJIA`, `DWWMF`, `DWOP`에 DCL 설정 창을 적용했습니다.
- 모든 창은 `DawooLISP_UI.dcl` 파일 하나로 통합했습니다.
- 기존 명령의 핵심 처리 방식과 설치·업데이트·신뢰 위치·시작세트·메뉴 등록 방식은 유지했습니다.

### 환경 및 도면 설정

- `DEF`에서 적용할 환경설정을 체크하여 선택 실행할 수 있습니다.
- `DEFLA`에서 생성할 레이어 분야를 목록으로 선택할 수 있습니다.
- `DEFSC`에서 기본 축척과 사용자 축척, 미사용 축척 정리를 설정할 수 있습니다.
- `DEFCTB`에서 출력 굵기표와 권장 세트를 확인한 뒤 세트1~세트4를 적용할 수 있습니다.

### 수치지도·연속지적도·SHP

- `DWNUM`에서 반경/사각영역, 좌표계, 결과 도면을 한 창에서 선택합니다.
- `DWJI`에서 법정주소/사각영역, 좌표계, 결과 도면을 한 창에서 선택합니다.
- `DWSHP`에서 출력 좌표계와 기존 파일 덮어쓰기를 설정합니다.
- `DWJIA`에서 지번의 최소·최대 글자 크기를 입력한 뒤 정리합니다.

### 복사 및 프로그램 관리

- `DWWMF`에서 객체 선택 방식과 색상 처리 방식을 선택합니다.
- `DWOP` 상단에 현재 설치 버전을 표시합니다.
- `DWOP`에서 설치 폴더, 백업 폴더, 버전 정보, 프로그램 다시 로드, 도구막대 표시·복구를 실행할 수 있습니다.
- `DWOP`에서 업데이트 채널 저장과 최신 버전 확인·업데이트를 실행할 수 있습니다.

### 1.7.17 긴급 수정

- `DEFCTB`, `DWJI`, `DWJIA`, `DWSHP`, `DWWMF` 실행 시 발생하던 `잘못된 함수: #<SUBR>` 오류를 수정했습니다.
- 기존 처리 함수를 내부 함수로 분리하여 AutoCAD와 ZWCAD에서 직접 안전하게 호출하도록 변경했습니다.
- `DWOP`의 설치 폴더·백업 폴더 열기 동작을 Windows 탐색기 호환 방식으로 수정했습니다.
- AutoCAD 2015~2024에서 지원되지 않는 `VL-SYMBOL-FUNCTION` 호출을 제거했습니다.
- `DWOP`에서 폴더 열기, 버전 정보, 도구막대 표시·복구, 채널 저장 후 설정 창이 다시 표시됩니다.
- 버전 정보는 별도의 안내 창으로 표시됩니다.

### 1.7.17 추가 기능 및 성능 개선

- `DWDT`에 문자 스타일·높이·주석형 선택 창을 적용했습니다.
- `DWDT`의 일반문자 높이를 창에서 지정하면 명령행에서 높이를 다시 묻지 않습니다.
- `DWJ`에 연결 허용거리 설정 창을 적용하고 마지막 값을 유지합니다.
- `DWJ`는 2D와 3D 객체를 자동 분류하며 3D 높이를 유지한 채 경사진 구간으로 연결합니다.
- `DWJIA`에서 필지 경계상자 선필터와 필지 면적 캐시를 적용해 지번 분석 속도를 개선했습니다.
- `DWJIA`의 지번 배치 계산과 결과 형식은 기존 방식을 유지합니다.

### 지원 환경

- AutoCAD 2015~2024 (Windows)
- ZWCAD 2024 (Windows)

공개베타 버전이므로 자동 업데이트를 받으려면 `DWOP`에서 업데이트 채널을 `공개베타`로 저장합니다.
