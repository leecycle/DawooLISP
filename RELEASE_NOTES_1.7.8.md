## DawooLISP 1.7.8 공개베타

### DWSHP · SHP 파일 생성 수정

- `DWSHP`를 지연 로딩 명령 목록에 등록했습니다.
- CAD를 시작한 직후에도 다른 DawooLISP 명령을 먼저 실행하지 않고 `DWSHP`를 바로 사용할 수 있습니다.
- 정상 확인된 Windows PowerShell 절대경로와 비대화형 실행 방식을 본 명령에 적용했습니다.
- 변환 시작·실패 원인·결과 파일 존재 여부를 명령줄에 표시합니다.

### DWNUM · 사각영역 외부 절단 보완

- 새 빈 도면을 활성화하고 재생성한 뒤 외부 절단을 실행합니다.
- EXTRIM 후 남은 외부 객체를 확인하고 기본 TRIM과 외부 객체 제거를 추가 실행합니다.
- 사각영역 밖에 객체가 남으면 완료로 표시하지 않고 잔여 개수를 경고합니다.

### DWJI · 실시간 진행률과 새 도면 출력

- 사각영역 실행 중 SHP 검색·대상 검사·필지 계산·좌표 추출 진행률을 표시합니다.
- 사각영역 결과를 DWNUM과 같이 새 빈 도면에 작성합니다.
- 새 도면에서 이번에 가져온 지번만 자동 정리하고 안내문을 작성합니다.

### 설치·업데이트 완료 페이지

- 신규 설치가 성공하면 DawooLISP GitHub 메인 페이지를 엽니다.
- 업데이트가 성공하면 설치된 `v1.7.8` 릴리스 페이지를 엽니다.
- 설치 또는 업데이트가 실패하거나 취소되면 웹페이지를 열지 않습니다.

### DWJI · 기존 기능 유지

- 기존 법정주소 가져오기 방식을 그대로 유지했습니다.
- 사각영역을 지정하면 영역과 교차하는 연속지적도 필지만 가져옵니다.
- 이번에 가져온 지적 경계와 지번 문자만 `DWJIA`로 자동 정리합니다.
- 독립 `DWJIA` 명령은 기존 선택 실행 방식 그대로 유지됩니다.

### DWNUM · 기존 기능 유지

- 기존 반경검색 방식을 그대로 유지했습니다.
- 사각영역을 지정하면 해당 도엽번호와 DXF를 검색합니다.
- 검색된 DXF를 새 빈 도면에 결합한 뒤 선택 사각형 외부를 절단합니다.

### 기존 핵심 구조 유지

- 1.7.7의 설치·업데이트·신뢰 위치·시작세트·PGP·메뉴 등록 방식은 변경하지 않았습니다.
- 기존 도구막대·메뉴·PNG 절대경로 구조를 유지했습니다.
- AutoCAD와 ZWCAD를 모두 종료한 뒤 `Install.exe`를 실행합니다.
- 공개베타 버전이므로 업데이트 채널을 베타버전으로 설정해야 자동 업데이트됩니다.
